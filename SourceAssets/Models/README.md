# Model workspace

Model sources are grouped by game responsibility:

- `Architecture`
- `Characters`
- `Environment`
- `Props`
- `Vehicles`
- `Vegetation`

Each category has local `Raw`, `Working`, and `Exports` folders. Keep source
units in metres, +Y up, and export runtime assets as validated GLB files with
explicit collision and LOD decisions.
