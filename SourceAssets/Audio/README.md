# Audio workspace

The current game audio is procedural Web Audio and ships no recordings. Use
local `Raw/` and `Working/` folders for future licensed recordings, and export
only compressed files that are referenced by the standalone web build.
